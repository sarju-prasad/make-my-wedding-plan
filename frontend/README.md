# Make My Wedding Plan — Frontend

Placeholder. Not scaffolded yet.

Planned stack (per `../doc/system_design_architecture.pdf`): Next.js +
TypeScript. First deliverable: the public app landing page (static, no
backend dependency) — see the root [CLAUDE.md](../CLAUDE.md) for how this
workspace fits into the monorepo.

A concrete scaffold proposal (Next.js version/router, styling approach,
folder structure, tooling) will be reviewed before any code lands here,
matching how `backend/` was built.
